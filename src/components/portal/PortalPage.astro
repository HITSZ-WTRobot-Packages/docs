---
import StarlightPage from "@astrojs/starlight/components/StarlightPage.astro";

import type { PortalSidebar } from "../../lib/site/navigation";

interface Props {
  title: string;
  description: string;
  sidebar: PortalSidebar;
  pagefind?: boolean;
  rightSidebar?: boolean;
}

const { title, description, sidebar, pagefind = true, rightSidebar = false } = Astro.props;
---

<StarlightPage
  frontmatter={{ title, description, pagefind, editUrl: false, tableOfContents: rightSidebar }}
  sidebar={sidebar}
  hasSidebar={true}
>
  <slot />
</StarlightPage>
